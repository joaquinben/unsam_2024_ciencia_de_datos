Datos del indec:

CODSU:Esta variables es categórica (c) ya que representa codigos para distingir viviendas, a vez es discreta ya que los valores son enteros y no son fracciones.No tiene un nivel de meicion especifico ya que no implica ningun orden ni una escala en sí misma. 

NRO_HOGAR: Tambien es categorica (N) ya que representa codigos para distingir hogares. Es discreta, ya que los valores son enteros y no fracciones. al igual que CODSU no poseé un nivel de medicion especifica.

REALIZADA: Es categorica (N) ya que representa la realizacion o no de una entrevista. en este caso podria tratarse de una como una variable, donde "si" representa un valor y "no" representa otro valor. Es discreta ya que se puede contabilizar el numero de veces que se responde "si" o "no", pero no posee valores intermedios. 

ANO4: Esta variable es numerica (N) ya que presenta el año de relevamiento con valores de 4 digitos. Es discreta, ya que los años son valores enteros y no fracciones. Ademas es variable de intervalo, ya que hay una distancia entre cada año, pero no tiene un punto cero absoluto. 
